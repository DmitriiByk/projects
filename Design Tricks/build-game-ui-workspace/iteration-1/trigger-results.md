# Триггеринг-аудит build-game-ui (ручной, 20 запросов)
Метод: claude -p в песочнице не залогинен → авто-loop недоступен. Вердикт вынесен судейски против текущего description.

## Should-trigger (10)
| # | запрос (суть) | вердикт | ок? |
|---|---|---|---|
|1|кнопка+состояния, figma-нода|TRIGGER|✓|
|2|инпут вдавленный + иконки в figma|TRIGGER|✓|
|3|карточка игры, бейдж, вырез угла|TRIGGER|✓|
|4|неоновый тоггл, responsive|TRIGGER|✓|
|5|прогресс-бар энергет. заливка|TRIGGER|✓|
|6|промо-БАННЕР, реюз растров, брейкпоинты|TRIGGER (слабо)|⚠ «баннер» нет в списке элементов|
|7|табы кремовые, реюз иконок|TRIGGER|✓|
|8|золотая CTA, hover сильнее|TRIGGER|✓|
|9|бейдж-пилюля, объём|TRIGGER|✓|
|10|панель-диалог, радиал+рамка|TRIGGER|✓|

## Should-NOT-trigger (10, near-misses)
| # | запрос (суть) | вердикт | ок? |
|---|---|---|---|
|11|React+tailwind кнопка, «без фигмы»|no-trigger|✓ (риск: слово «кнопка»)|
|12|флоучарт в figjam (mermaid)|no-trigger|✓|
|13|извлечь токены/обновить переменные|no-trigger|✓|
|14|презентация pptx|no-trigger|✓|
|15|починить баг в figma-плагине (ts)|no-trigger|✓ (риск: «figma»)|
|16|ресайз картинок @2x webp|no-trigger|✓|
|17|ux-ревью экрана|no-trigger|✓|
|18|генеративный арт p5.js|no-trigger|✓|
|19|дашборд метрик выручки|no-trigger|✓|
|20|одиночная иконка-шестерёнка 24×24 svg|no-trigger|✓ (риск: «иконочные плитки»)|

## Счёт (судейский): 20/20.
## Слабые места (что поймал бы эмпирический loop):
1. UNDER: «баннер/составной макет» не в списке элементов (q6) — хотя по факту делаем. → добавить в описание.
2. OVER-риск near-miss: q11 (код-компонент), q15 (отладка плагина), q20 (плоская иконка) — общие ключевые слова. → добавить негативную область.
3. ПЕРЕСЕЧЕНИЕ build-game-ui ↔ build-game-button: все «кнопочные» запросы (q1,q8) триггерят ОБА. build-game-button стал строгим подмножеством build-game-ui (T-каталоги почти идентичны: T1–T19 совпадают). → рекомендация: либо вывести button из эксплуатации, либо явно сузить его описание до «только кнопки», чтобы не конкурировал.

## Прогон 2 — независимые судьи-субагенты (вслепую, после правок описаний)
Движок: claude -p в песочнице не залогинен (нет токена/ключа) → вместо run_loop подняты 2 независимых агента-классификатора (RU+EN), не видевших мои оценки. Выбор между A=build-game-ui / B=build-game-button / neither.

Результат: оба судьи дали ИДЕНТИЧНЫЙ расклад, совпавший с эталоном 20/20.
- should-trigger (1–10): все A/B (recall 10/10). Маршрутизация ui↔button: q1,q8 → B (кнопки), q2–q7,q9,q10 → A (инпут/карточка/тоггл/прогресс/баннер/табы/бейдж/панель).
- should-NOT (11–20): все «neither» (specificity 10/10). Негативная область отработала: код-компонент(11), FigJam(12), токены(13), pptx(14), отладка плагина(15), ресайз(16), ux-ревью(17), p5-арт(18), дашборд(19), плоская иконка(20).
- Согласие судей между собой: 20/20 (kappa≈1.0), средняя уверенность ~0.94.

ВЫВОД: правки описаний валидны независимо. Баннер(q6)→A подтверждает добавление «баннеры/составные макеты»; сужение button убрало конкуренцию (кнопки уверенно → B, прочие элементы → A).
